package com.aditya1875.cashyndoapp.ui.theme

import androidx.compose.ui.graphics.Color

// ── Cashyndo Brand Colors (from logo) ──────────────────────────────────────
val BrandTeal        = Color(0xFF1A7A7A)   // logo primary teal
val BrandTealLight   = Color(0xFF23A0A0)   // lighter teal for accents / cyan glow
val BrandAmber       = Color(0xFFF5A623)   // logo amber wedge
val BrandAmberLight  = Color(0xFFFFBF4D)

// ── Dark Surface Palette ───────────────────────────────────────────────────
val DarkBackground   = Color(0xFF0D1117)   // near-black OLED bg
val DarkSurface      = Color(0xFF161C24)   // card surface
val DarkSurfaceVar   = Color(0xFF1E2730)   // slightly lighter card

// ── On-Colors ─────────────────────────────────────────────────────────────
val OnDark           = Color(0xFFE8EDF2)
val OnDarkMuted      = Color(0xFF8A9BAD)

// ── Semantic ──────────────────────────────────────────────────────────────
val IncomeGreen      = Color(0xFF00C07F)
val ExpenseRed       = Color(0xFFFF4D6A)

// ── Light Surface Palette ─────────────────────────────────────────────────
val LightBackground  = Color(0xFFF4F7FA)
val LightSurface     = Color(0xFFFFFFFF)
val OnLight          = Color(0xFF0D1117)
val OnLightMuted     = Color(0xFF6B7A8D)